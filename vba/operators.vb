Dim A As Integer, B As Integer, C As Integer
Dim D As Boolean

' math
C = A + B * B - A / C
C = A % B \ A ^ C

' comparison
D = C <> B > A <= A >= B < C

' logical
D = A imp B eqv C
D = A and B or C xor D

Dim A As Integer, B As Integer, C As Integer
Dim D As Boolean

' math
C = (A + B) * B - A / C
C = A Mod B \ A ^ C

' comparison
D = C <> B > A <= A >= B < C

' logical
D = A And B Or C Xor D
D = Not A And Not B
